{
  "name": "cabt-replay-viewer",
  "private": true,
  "description": "Public CABT replay viewer with a FastAPI Kaggle integration backend.",
  "scripts": {
    "dev:web": "npm --prefix web run dev:web",
    "build:web": "npm --prefix web run build",
    "test:web": "npm --prefix web test",
    "test:e2e": "npm --prefix web run test:e2e",
    "test:api": "python3 -m pytest",
    "test": "npm run build:web && npm run test:web && npm run test:api"
  }
}
