# Web

Svelte replay UI for CABT Replay Viewer.

Run from the repository root:

```bash
npm --prefix web ci
npm --prefix web run dev
```

The Vite dev server proxies `/api` to `http://localhost:8000` by default.
